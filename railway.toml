[build]
builder = "DOCKERFILE"
dockerfilePath = "heartbeat-engine/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"

[[deploy.volumes]]
mountPath = "/app/desktop-builds"
name = "desktop-builds"